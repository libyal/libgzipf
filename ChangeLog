To do:
* compressed segment support
** add support for compressed segment descriptor
** use compressed segment descriptor to read compressed segment
** if file size < compressed segment size, read file entirely into memory?

* fix deflate fallback for more complex gzip files
* member
** tracked uncompressed data offset
* gzipfinfo
** print information of members, such as name, comments, modification time
* add bit stream with read on demand support?
* member header add support for extra fields controlled by flags
* complete library
* complete Python bindings
* debug pring compression flags
* debug print operating system

20190716
* see `git log' for more recent change log
* initial version based on liblnk
